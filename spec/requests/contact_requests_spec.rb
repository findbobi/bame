 require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to test the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator. If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails. There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.

RSpec.describe "/contact_requests", type: :request do
  
  # ContactRequest. As you add validations to ContactRequest, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  describe "GET /index" do
    it "renders a successful response" do
      ContactRequest.create! valid_attributes
      get contact_requests_url
      expect(response).to be_successful
    end
  end

  describe "GET /show" do
    it "renders a successful response" do
      contact_request = ContactRequest.create! valid_attributes
      get contact_request_url(contact_request)
      expect(response).to be_successful
    end
  end

  describe "GET /new" do
    it "renders a successful response" do
      get new_contact_request_url
      expect(response).to be_successful
    end
  end

  describe "GET /edit" do
    it "render a successful response" do
      contact_request = ContactRequest.create! valid_attributes
      get edit_contact_request_url(contact_request)
      expect(response).to be_successful
    end
  end

  describe "POST /create" do
    context "with valid parameters" do
      it "creates a new ContactRequest" do
        expect {
          post contact_requests_url, params: { contact_request: valid_attributes }
        }.to change(ContactRequest, :count).by(1)
      end

      it "redirects to the created contact_request" do
        post contact_requests_url, params: { contact_request: valid_attributes }
        expect(response).to redirect_to(contact_request_url(ContactRequest.last))
      end
    end

    context "with invalid parameters" do
      it "does not create a new ContactRequest" do
        expect {
          post contact_requests_url, params: { contact_request: invalid_attributes }
        }.to change(ContactRequest, :count).by(0)
      end

      it "renders a successful response (i.e. to display the 'new' template)" do
        post contact_requests_url, params: { contact_request: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "PATCH /update" do
    context "with valid parameters" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested contact_request" do
        contact_request = ContactRequest.create! valid_attributes
        patch contact_request_url(contact_request), params: { contact_request: new_attributes }
        contact_request.reload
        skip("Add assertions for updated state")
      end

      it "redirects to the contact_request" do
        contact_request = ContactRequest.create! valid_attributes
        patch contact_request_url(contact_request), params: { contact_request: new_attributes }
        contact_request.reload
        expect(response).to redirect_to(contact_request_url(contact_request))
      end
    end

    context "with invalid parameters" do
      it "renders a successful response (i.e. to display the 'edit' template)" do
        contact_request = ContactRequest.create! valid_attributes
        patch contact_request_url(contact_request), params: { contact_request: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "DELETE /destroy" do
    it "destroys the requested contact_request" do
      contact_request = ContactRequest.create! valid_attributes
      expect {
        delete contact_request_url(contact_request)
      }.to change(ContactRequest, :count).by(-1)
    end

    it "redirects to the contact_requests list" do
      contact_request = ContactRequest.create! valid_attributes
      delete contact_request_url(contact_request)
      expect(response).to redirect_to(contact_requests_url)
    end
  end
end
